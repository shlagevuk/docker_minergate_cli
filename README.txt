This is a simple image containing minergate_cli. 

docker run -d --name some-minergate-cli shlagevuk/minergate_cli --user your.email@gmail.com --xmr

More information with:

docker run --rm shlagevuk/minergate_cli -help
